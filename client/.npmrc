# Handle rollup optional dependencies
optional=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Don't fail on missing optional dependencies
optional-deps=true
# Ensure platform-specific packages are installed
target_platform=linux
target_arch=x64
